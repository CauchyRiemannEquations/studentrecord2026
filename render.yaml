services:
  - type: web
    name: school-record-qa-bot
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python -m app.server
    plan: free
    autoDeploy: true
